{
  "buildCommand": "cd backend && npm install && cd ../frontend && npm install && CI=false npm run build",
  "outputDirectory": "frontend/build",
  "alias": ["blog-platform-nine-eosin.vercel.app"]
}
